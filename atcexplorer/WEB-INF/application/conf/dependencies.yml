# Application dependencies

require:
    - play
    - provided -> owlapi 3.2.4
    - provided -> log4j 1.0
    - provided -> elk 0.2
    
repositories: 
    - provided: 
        type:       local 
        artifact:   "${application.path}/jar/[module]-[revision].jar" 
        contains: 
            - provided -> * 
    